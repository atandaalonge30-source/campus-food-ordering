# Vercel environment variables for the Campus Food Ordering API

# If you have a single MySQL connection URL, set DATABASE_URL:
DATABASE_URL=mysql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:3306/<DB_NAME>

# Or set these individually (preferred if your provider gives separate values):
DB_HOST=<DB_HOST>
DB_PORT=3306
DB_USER=<DB_USER>
DB_PASSWORD=<DB_PASSWORD>
DB_NAME=<DB_NAME>
DB_SSL=false # set to true if your DB requires SSL connections

# The client URL (for CORS)
CLIENT_URL=https://<YOUR_FRONTEND_URL>

# Optional: any admin account for seeding (seed script uses ADMIN_* envs)
ADMIN_EMAIL=admin@tpi.edu.ng
ADMIN_PASSWORD=Password123!
ADMIN_NAME=System Administrator
